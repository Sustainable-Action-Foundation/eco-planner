tests/

# Prisma generated client (regenerated in the prisma stage)
prisma/generated/

docker/

# Dependencies
node_modules/

# Build outputs
.next/

# Test output
test-results/
playwright-report*/

# Environment variables (security)
.env*

# Logs
*.log

# Documentation
README*
LICENSE
*.md

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo

# OS generated files
.DS_Store
Thumbs.db

# Git
.git/
.github/
.gitignore

# Caches (stray changes here would bust the COPY layer cache)
*.tsbuildinfo
.eslintcache
# Exclude yarn state but keep patches, which yarn install needs
.yarn/*
!.yarn/patches/

# Local scratch and agent worktrees
.claude/
ignore/
local.gitignore
